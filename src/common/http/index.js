import * as api from './api.js';
import {httpService,currentHost,http} from './http.js';
//console.log('api ---- ', api,httpService,currentHost,http)
export {
    api,
    httpService,
    http,
    currentHost
}
